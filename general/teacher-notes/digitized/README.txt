Digital replicas produced from the scans live here (docx, xlsx, pptx, html, quizzes...).
Register each one on its source page in manifest.json under digitized: [{"label": "...", "file": "digitized/<name>"}].
